import numpy as np

def max_all(arr: np.ndarray) -> int:
    """
    Returns the max of all numbers in the array
    Args:
        arr: np.ndarray
    Returns:
        int: max of all numbers
    """
    return np.max(arr)