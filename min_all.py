import numpy as np

def min_all(arr: np.ndarray) -> int:
    """
    Returns the min of all numbers in the array
    Args:
        arr: np.ndarray
    Returns:
        int: min of all numbers
    """
    return np.min(arr)